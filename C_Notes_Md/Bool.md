# 布尔值
```C
#include <stdbool.h>
#include <stdio.h>

int main() {
  bool isProgrammingFun = false;
  if (isProgrammingFun) {
    printf("Great");
  } else {
    printf("Not great");
  }
}
```
你需要引入`<stdbool.h>`来设置一个bool
剩余用法基本与`Python`一致
